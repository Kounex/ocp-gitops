apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous

resources:
  # - sealed-secret-htpasswd.yml
  - external-secret-htpasswd.yml
  - oauth.yml
