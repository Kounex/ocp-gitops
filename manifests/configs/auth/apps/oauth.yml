kind: OAuth
apiVersion: config.openshift.io/v1
metadata:
  name: cluster
  annotations:
    argocd.argoproj.io/sync-wave: '150'
spec:
  identityProviders:
    - name: Local
      mappingMethod: claim
      type: HTPasswd
      htpasswd:
        fileData:
          name: htpasswd
